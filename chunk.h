#ifndef CHUNK_H#define CHUNK_H#include <QtWidgets>#include <QGLWidget>#include "Tree.h"const int MAP_SIZE = 33;class Chunk{public:	enum CellType {EMPTY, BUSH, TREE};	enum Side {Left, Bottom, Right, Top};		void setSide(Side side, GLfloat data[MAP_SIZE][MAP_SIZE]);	void generate();	void generateQuad(int x, int y, int size);	void clear();	void operator=(const Chunk& chunk);	GLfloat heightMap[MAP_SIZE][MAP_SIZE];	CellType cellType[MAP_SIZE][MAP_SIZE];	QVector <Tree> trees;	QVector <QPointF> treesCoord;		static GLfloat maxDeltaHeight;	static GLfloat maxHeight;	static int maxTreesNum;	static int maxGrassNum;};#endif